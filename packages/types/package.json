{
  "name": "@tasknest/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types & Zod schemas across api/web/mobile",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "node -e \"console.log('@tasknest/types build — pending')\"",
    "test": "node -e \"console.log('@tasknest/types test — pending')\"",
    "test:unit": "node -e \"console.log('@tasknest/types test:unit — pending')\"",
    "lint": "node -e \"console.log('@tasknest/types lint — pending')\"",
    "typecheck": "node -e \"console.log('@tasknest/types typecheck — pending')\"",
    "clean": "node -e \"console.log('@tasknest/types clean')\""
  }
}
